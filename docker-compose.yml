version: "3.8"

services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./alb_data.db:/app/alb_data.db
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
