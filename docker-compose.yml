version: "3.8"

services:
  dashboard:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./alb_data.db:/app/alb_data.db
    restart: unless-stopped
    environment:
      - PORT=5000
